import styled from '@emotion/styled';
import { inner } from '@/styles/common.styles';

export const CartFooterBottom = styled.div`
    ${inner};
`;

export const ExpireCartText = styled.p`
    strong {
        border-bottom: 1px solid #fff;
    }
`;
